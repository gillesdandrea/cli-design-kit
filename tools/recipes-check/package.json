{
  "name": "recipes-check",
  "version": "0.1.0",
  "description": "Extract TypeScript snippets from recipes.md and typecheck them.",
  "type": "module",
  "scripts": {
    "extract": "bun run src/extract.ts",
    "check": "bun run src/check.ts"
  },
  "dependencies": {
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
